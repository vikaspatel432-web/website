# Team photos

Drop Yogiraj's headshot here as **`yogiraj.jpg`** (a square or portrait crop
works best; it renders in a 160×192 rounded frame, `object-cover` from the top).

Until the file exists, the About → Leadership card falls back to his initials,
so nothing looks broken. PNG also works — if you use `yogiraj.png`, update the
`photo` path in `src/content.ts`, or just send me the file and I'll wire it.
